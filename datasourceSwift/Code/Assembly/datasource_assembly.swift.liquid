//
//  {{ module_info.file_name }}
{% include 'header' %}

import Swinject


final class {{ module_info.name }}DataSourceAssembly: Assembly {

    func assemble(container: Container) {

        container.register({{ module_info.name }}DataSource.self) { r in
            let datasource = {{ module_info.name }}DataSourceImpl()

            // MARK: insert here internal dependencies

            return datasource
        }
    }
}
