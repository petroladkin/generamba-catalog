//
//  {{ module_info.file_name }}
{% include 'header' %}

import Swinject


final class {{ module_info.name }}RepositoryAssembly: Assembly {

    func assemble(container: Container) {

        container.register({{ module_info.name }}Repository.self) { r in
            let repository = {{ module_info.name }}RepositoryImpl()

            // MARK: insert here internal dependencies

            return repository
        }
    }
}
