//
//  {{ module_info.file_name }}
{% include 'header' %}

import Quick
import Nimble
import Swinject

@testable import {{ module_info.project_name }}


class {{ module_info.name }}ServiceImplSpec: QuickSpec {
    
    override func spec() {
        describe("{{ module_info.name }}Service") {
            let assembler = Assembler([{{ module_info.name }}ServiceSpecAssembly()])
            var service: {{ module_info.name }}Service!
            beforeEach {
                service = assembler.resolver.resolve({{ module_info.name }}Service.self)!
            }
            
            afterEach {
            }
            
            context("[insert here contex name]") {

                it("[insert here it name]") {
                    
                }
            }
        }
    }
}
